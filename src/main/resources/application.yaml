spring:
  application:
    name: smart-door-lock-be

  mqtt:
    username: alice
    password: alice123456
    url: tcp://52.169.3.167:1883
    pubClientId: pub_spring
    subClientId: sub_spring
    #    多个订阅主题用`,`隔开
    subTopic: device/lock/#

  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #    url: jdbc:mysql://127.0.0.1:3306/smart_door_lock?useSSL=false&serverTimezone=UTC
    url: jdbc:mysql://34.249.191.107:3306/smart_door_lock
    username: root
    password: iotcs7ns2

  data:
    redis:
      host: localhost
      port: 6379
  cache:
    redis:
      # 缓存过期时间
      time-to-live: 10h

server:
  port: 80

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:mapper/*Mapper.xml

jwt:
  private.key: classpath:app.key
  public.key: classpath:app.pub